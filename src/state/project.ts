/**
 * プロジェクト自身についての事実（2026-09-06）。
 *
 * **URL を描画層に直接書かない。** 配信元は一度変わっている
 * （2026-09-03 に専用 Organization `mieru-app/mieru` へ移した）。
 * 次に変わったときに直す場所を1つにしておく。
 *
 * **ここが指す先は飾りではない。** 「全てのコードはこのリポジトリにある。
 * 疑うなら読める」という説明が、本ツールの信用の説明そのものである
 * （`SECURITY.md`、設計書 8.6）。**画面から辿れなければ、その説明は
 * 読んだ人にしか届かない。** 設定から1回で行ける場所に置く。
 */

/**
 * ソースコードのリポジトリ。
 *
 * **`package.json` の `repository` と一致していることをテストで縛る**
 * （`__tests__/project.test.ts`）。ここが実際の置き場所とずれると、
 * 確かめに来た人を別の場所へ送ることになる。
 */
export const SOURCE_URL = "https://github.com/mieru-app/mieru";
