# キャンバスの余白と中央寄せ（2026-09-04・未解決）

**種別: アイディアドキュメント（記録）。正本ではない。**
確定した内容は `docs/design/` にある。ここは「なぜそう決めたか」を残す場所であり、
書かれた時点の事実である。**現在の仕様として読まないこと。**

**2026-09-05 に決着した。** 実測へ切り替えたら2往復で終わった。
2026-09-04 の4回はすべて推測であり、ブラウザで寸法を1度も測っていなかった。

---

## 決着（2026-09-05）

**原因は1つだった。`.map-container` の高さが、見えている高さと違っていた。**

| | `.canvas`（見えている） | `.map-container`（`mind-elixir` の入れ物） |
|---|---|---|
| 畳んだ状態 | 835 | **1472** |
| 展開した状態 | 835 | **3794** |

`mind-elixir` の CSS は `.map-container { height: 100% }` だが、
**親が確定した高さを渡していないと解決せず、内容なりの高さへ落ちる。**

**症状3つは、すべてこの1つから出ていた。**

| 症状 | 仕組み |
|---|---|
| 中央に来ない | `centerMap()` は `mind.container` を物差しにする。3794 の箱の中心へ**正しく**置いた結果、中心テーマが画面の 1480px 下に沈む |
| ⛶ で縦に縮まない | `scaleFit()` の縦比は `nodes / container`。入れ物が内容なりだと比が常に約1になる |
| 下に「地図の外」が残る | `overflow: hidden` が見えている端で切れない |

**`centerMap()` の式は正しかった。** 実測では container の中心 y=1945 と
root の中心 y=1945 がぴったり一致していた。**物差しが違っただけである。**

## 直し方も推測せず、画面上で選んだ

候補を3つ console で当て、`container.h == canvas.h` になるものを見た。

| 候補 | 結果 |
|---|---|
| A: `.map-container` を `position: absolute; inset: 0` | ❌ **高さが 0 になった** |
| B: `.canvas` を格子にして行と列を明示する | ✅ 835 |
| C: `.map-container` に実寸を JS で入れる | ✅ 835（対症療法なので不採用） |

**A が 0 になったことが手がかりだった。** 中身を流れから外した瞬間に
高さが消えた＝**`.canvas` の高さは中身に依存していた。**
外から与えるしかないので、格子の行で渡す B を採った。

採用したのは `src/app/styles.css` の `.canvas` に
`display: grid` と `grid-template-rows: minmax(0, 1fr)` と
`grid-template-columns: minmax(0, 1fr)` を足すこと。
**列も明示する。** `me-nodes` は `width: max-content` なので、
暗黙の `auto` 列だと横に広い地図で入れ物ごと伸びる。

**2026-09-04 の4回目（`.workarea` に `grid-template-rows` を足す）は正しかった。**
壊れていたのが2段あり、1段目だけ直して直った気になっていた。

## 教訓（この一件から）

- **実測は「症状を測る」だけでなく「直し方を選ぶ」のにも使える。**
  候補を console で当てれば、コードを書く前にどれが効くか分かる
- **同じ種類の壊れ方が2段あることがある。** 1段直して症状が変わらないとき、
  「その直し方が間違っていた」と結論を急がない

---

## 以下は 2026-09-04 時点の記録（当時は未解決だった）


**M2.8 の後の実利用期間で出た2件。** 計画（5章）はマイルストーン間に実利用期間を挟むと
定めており、**使ってみて出たことは次の工程（2.9）より優先する。**

| ID | タスク | 成果物 | 工数目安 | 依存 |
|---|---|---|---|---|
| ~~2.11-1~~ | ~~一覧と欄の幅を掴んで変えられるようにする~~ **2026-09-04 完了** | `src/state/pane-size.ts` | 0.5日 | — |
| ~~2.11-2~~ | ~~要素が少ないときにキャンバスの下が余る件~~ **2026-09-05 完了**（原因と決め手は上記） | `src/app/styles.css` | 4往復＋2往復 | — |
| | | **小計** | **約1日** | |

**2.11-1 の判断は `src/state/pane-size.ts` にある。** 可動域・既定・保存の読み方に加え、
**掴んだ指を右へ動かしたときにどちらが広がるかを持つ。** 一覧（左）と欄（右）で符号が
逆になり、取り違えると掴んだ向きと逆に伸びる欄ができる。

- 保存先は `localStorage`。**マップごとの表示状態ではなく端末ごとの好み**なので、
  frontmatter の `mm:` には書かない（不変条件2）。配色（`theme.ts`）と同じ扱い
- **境目は格子の列にせず、上に重ねて置く**（`.pane-resizer`）。列として挟むと
  `grid-template-columns` が4通りとも変わり、狭い画面の畳み方（2.7-1）まで波及する
- 狭い画面には出さない。あちらは列ではなく覆いなので、動かす幅が無い
- 掴めない環境のために矢印キーでも動かせる（`Home` で既定へ戻る）。原則4

**2.11-2 は 2026-09-04 時点では未解決だった。**
4つの原因を立てて4回直したが、**実機では症状が残っている。**
以下は「分かっていること」「試して効かなかったこと」「次にやること」の記録である。

**症状**

- 子・孫を畳んだ状態では中心テーマが中央にあるように見える
- **展開すると中央に来ない。** ⛶ を押しても来ない
- サイドバーで別のマップに切り替えても来ない
- 主表示の下に「地図の外」が残る
- 入力中に地図が飛ぶことは無い（この点だけは保てている）

**実測して確かなこと**

- `.map-canvas` は `width: fit-content` で高さも内容なり。**その外側は
  `.map-container` の背景がそのまま見える**
- `mind-elixir` の暗色テーマは `--bgcolor: #252526`、こちらは `--bg: #16181d`。
  **スクリーンショットに出る境目は `.map-container` の下端を指している**
- 境目の位置は**内容の高さに追随する**（畳むと上がり、展開すると下がる）
- `refresh()` は `layout()` と `linkDiv()` だけで**中央寄せをしない**。
  マップの切り替えは `refresh()` を通る
- `toCenter()` は `transform-origin` を縦 50% に固定したまま `offsetTop` から
  位置を組み立てる。**拡大率が 1 でなく、中心テーマが地図の縦中心にないとずれる**
- `scaleFit()` の縦比は `nodes.offsetHeight / container.offsetHeight` で、
  `n = 1 / Math.max(1, ...)`。**入れ物が内容なりだと比が約1になり縮小が掛からない**

**試して効かなかったこと**（コミット `d0ed415` `6541f7d` `4fdb3da`）

1. `ResizeObserver` で大きさが変わったら中央へ寄せ直す
2. `scaleFit()` の後に入れ物の `scrollTop` / `scrollLeft` を戻す
3. 中央寄せを式ではなく `getBoundingClientRect()` で測って `move()` で詰める（`centerMap()`）
4. `.workarea` に `grid-template-rows: minmax(0, 1fr)` を足して高さの連鎖を確定させる

**次に着手する人がまずやること: 推測で直さない。実測値を取る。**

**この4回はすべて「コードを読んで立てた仮説」であり、ブラウザでの寸法を1度も
測っていない。** それが遠回りの原因である。開発者ツールの console で次を実行し、
**数値を記録してから**手を入れること。

```js
const c = document.querySelector('.canvas');
const m = document.querySelector('.map-container');
const n = document.querySelector('me-nodes');
console.table({
  canvas: c.getBoundingClientRect(),
  container: m.getBoundingClientRect(),
  nodes: n.getBoundingClientRect(),
  scroll: { top: m.scrollTop, left: m.scrollLeft },
  transform: getComputedStyle(document.querySelector('.map-canvas')).transform,
});
```

**見るべき点**

- `container` の高さが `canvas` と一致しているか（違えば高さの連鎖がまだ切れている）
- `nodes` の中心が `container` の中心と一致しているか（違えば位置の問題）
- `transform` の scale がいくつか（1 のままなら `scaleFit()` が効いていない）

**上流へ出すことも選択肢である。** `refresh()` が中央寄せをしないことと、
`scaleFit()` が scroll を戻さないことは、**`mind-elixir` 側の非対称**である。
5.15.1 に固定している以上こちらで包む必要はあるが、原因の切り分けには使える。

**教訓（この一件から）**

- **画面の色の差は寸法の手がかりになる。** 「無駄な黒い余白」と見えていたものは
  装飾ではなく要素の境界だった。「空いている」で済ませず、
  **どの要素の背景が見えているのか**を問うこと
- **IMPORTANT: 描画層の不具合を、コードを読むだけで直そうとしない。**
  この層は自動テストを持たない。**仮説を立てるたびに利用者に確認を依頼することになり、
  4往復かかった。** 先に実測値を取れば1往復で済んだ

---
